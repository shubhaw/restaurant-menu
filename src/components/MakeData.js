import React from "react";
// import data from "../assets/db/food.json";
// import menu from "../assets/db/db.json";

const MakeData = () => {
    // console.log(data.length);
    // for(let foodmenu of menu.foodMenu) {
    //     for(let submenu of foodmenu.submenu) {
    //         console.log(submenu.items.length);
    //     }
    // }

    // let updatedData = data.map(item => {
    //     if(item.Category === "Desserts" && item["Sub Category"] === "None")
    //     return {
    //         "name": item["Item Name"],
    //         "price": item["Item Price"],
    //         "speciality": item["Item Speciality"],
    //         "details": item["Item Details"]
    //     }
    // });
    // updatedData = updatedData.filter(item => !!item);
    // console.log(updatedData);
    return <div>

    </div>
}

export default MakeData;